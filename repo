Implement an orchestrator that does intent-based routing to call only the most relevant of GitLab/SharePoint/Jam first, then auto-escalates to additional agents only if confidence is low, results are missing/conflicting, or the query is high-stakes. Require each agent to return structured evidence (snippets + metadata + confidence) so the orchestrator can filter irrelevant items, dedupe/rerank, and answer immediately on a high-confidence early result without waiting for slow agents.
